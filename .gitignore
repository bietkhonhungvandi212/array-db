# ---- Go build artifacts ----
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Build directories
bin/
build/
dist/
out/

# go build and test caches (local-only)
*.out
*.cover
*.cov
*.coverprofile
*.pprof
coverage/
profile.out

# Module/package caches (never commit)
vendor/                    # if using 'go mod vendor' selectively, remove this ignore
.gopath/
.godeps/
.pkg/
*.a

# Generated code
gen/
generated/
*.gen.go
*_mock.go
mock_*.go

# Go workspaces
go.work
go.work.sum

# Local tool state (gopls, linters, etc.)
.golangci.yml.local
.golangci-cache/
.gocache/
.godev/
.gotools/
.gosumcheck/

# Delve debugger
__debug_bin
debug.test

# ---- Editors / IDEs ----
# Cursor / VS Code
.vscode/
.cursor/
*.code-workspace
.history/
.code-profile/

# JetBrains (GoLand)
.idea/
*.iml

# ---- OS-specific clutter ----
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# ---- Dependency / package managers (optional) ----
# If using these, keep their locks; otherwise ignore as needed
# npm / pnpm / yarn (for mixed repos with web assets)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
